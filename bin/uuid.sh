#!/bin/bash
# generate uuid

set -euo pipefail

echo "generate uuid"

# new flag: --debug

# simpler control flow

# return correct exit code on error

# safe to run multiple times

# new flag: --json
